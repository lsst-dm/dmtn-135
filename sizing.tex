\section{Sizing inputs}

The following simplified sizing was derived from \citeds{LDM-144} and was used to give the input sizes
for the cost model in \secref{sec:cost}.
The disk sizes are given in \tabref{tab:storageSizing} while the compute is given in \tabref{tab:computeSizing}.

\input{storageSizing}

\input{computeSizing}

